DirectoryIndex index.php index.html
<IfModule mod_rewrite.c>
  RewriteEngine On
  # Let actual files run; no SPA rewrites here.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
</IfModule>

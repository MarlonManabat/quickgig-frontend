export function initFacebook(appId: string) {
  if (typeof window === 'undefined') return;
  // eslint-disable-next-line @typescript-eslint/no-unused-vars -- FB SDK init pending
  void appId;
  // TODO: real FB SDK init
}
